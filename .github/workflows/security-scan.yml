name: Security Scan

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 2 * * 1'  # Weekly Monday 2 AM scan

jobs:
  versions:
    uses: ./.github/workflows/central-versions.yml

  security-scan:
    needs: versions
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@${{ needs.versions.outputs.actions_checkout_version }}

      - name: Set up JDK 25
        uses: actions/setup-java@${{ needs.versions.outputs.actions_setup_java_version }}
        with:
          distribution: temurin
          java-version: '25'

      - name: Cache Gradle packages
        uses: actions/cache@${{ needs.versions.outputs.actions_cache_version }}
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties', 'gradle/deps.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # Dependency vulnerability scanning with OWASP Dependency Check
      - name: OWASP Dependency Check
        uses: dependency-check/action@${{ needs.versions.outputs.dependency_check_action_version }}
        with:
          project: 'Kafka Transactional Microservices'
          path: '.'
          format: 'ALL'
          out: 'reports'
          args: >
            --disableAssembly
            --disableRetireJS
            --disableNodeJS
            --disableNodeAudit
            --disableNuspec
            --disableNexus
            --disableCmake
            --disableCocoapodsAnalyzer
            --disableSwiftPackageManagerAnalyzer

      - name: Upload dependency scan results
        uses: actions/upload-artifact@${{ needs.versions.outputs.actions_checkout_version }}
        if: always()
        with:
          name: dependency-scan-results
          path: reports/

      # Run SpotBugs if configured
      - name: Run SpotBugs analysis
        run: |
          if ./gradlew tasks | grep -q "spotbugs"; then
            ./gradlew spotbugsMain spotbugsTest --configuration-cache
          else
            echo "SpotBugs plugin not configured"
          fi

      # Run Error Prone if configured
      - name: Run Error Prone analysis
        run: |
          if ./gradlew tasks | grep -q "errorprone"; then
            ./gradlew compileJava compileTestJava --configuration-cache
          else
            echo "Error Prone plugin not configured"
          fi

      # Run Trivy vulnerability scan
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@${{ needs.versions.outputs.aquasecurity_trivy_action_version }}
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'sarif'
          output: 'trivy-results.sarif'
          exit-code: '0'  # Don't fail the build on vulns
          ignore-unfixed: true
          severity: 'CRITICAL,HIGH,MEDIUM'

      - name: Upload Trivy scan results
        uses: actions/upload-artifact@${{ needs.versions.outputs.actions_checkout_version }}
        if: always()
        with:
          name: trivy-scan-results
          path: trivy-results.sarif

      # SAST scan with CodeQL
      - name: Initialize CodeQL
        uses: github/codeql-action/init@${{ needs.versions.outputs.github_codeql_action_version }}
        with:
          languages: java-kotlin
          queries: +security-and-quality

      - name: Autobuild
        uses: github/codeql-action/autobuild@${{ needs.versions.outputs.github_codeql_action_version }}

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@${{ needs.versions.outputs.github_codeql_action_version }}
        with:
          category: "/language:java-kotlin"