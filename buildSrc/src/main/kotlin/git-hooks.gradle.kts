// Disabled: apply org.danilopianini.gradle-pre-commit-git-hooks in settings.gradle.kts per plugin guidance
// This precompiled script plugin is intentionally left empty to avoid build failures during tests.
